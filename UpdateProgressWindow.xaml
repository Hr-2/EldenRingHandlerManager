<Window x:Class="ERHandlerManager.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Updating"
        Width="430" Height="150"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="{StaticResource BgBrush}"
        Foreground="#FFFFFFFF"
        FontFamily="Segoe UI"
        TextElement.FontSize="12">
    <Grid Margin="20,18,20,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock x:Name="StatusText" Text="Preparing update..." Foreground="White" FontSize="12" FontWeight="SemiBold"
                       Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <ProgressBar Grid.Row="1" x:Name="ProgressBar" Height="6" Minimum="0" Maximum="100"
                     Margin="0,16,0,0" Background="#FF2D2D2D"
                     Foreground="{StaticResource AccentBrush}" BorderThickness="0"/>

        <TextBlock Grid.Row="2" x:Name="PercentText" Text="" Foreground="{StaticResource DimTextBrush}"
                   FontSize="10.5" Margin="0,8,0,0"/>
    </Grid>
</Window>
